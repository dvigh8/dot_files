# Save tmux history to zsh history
set -g history-file ~/.zsh_history

# Enable mouse support
set -g mouse on

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

bind r source-file ~/.tmux.conf

